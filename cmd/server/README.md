# Server 包

该包是应用程序的主要入口点，负责初始化和启动Web服务器。

## 功能
- 初始化路由
- 配置中间件
- 启动多个HTTP服务器实例（使用errgroup进行并发管理）
- 优雅处理服务器启动和关闭

## 主要函数
- `Main()` - 服务器主函数，负责设置和启动所有HTTP服务

## 使用方式
根目录的main.go文件导入并调用此包的Main函数来启动应用程序。