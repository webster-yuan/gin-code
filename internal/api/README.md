# API 包

该包包含应用程序的核心API功能，包括路由配置和HTTP处理程序的组织。

## 功能
- 定义和配置所有HTTP路由
- 设置模板和静态文件
- 提供额外服务器实例的路由配置

## 主要函数
- `SetupRouter()` - 设置主路由引擎，配置所有API端点
- `Router01()` - 返回第一个额外服务器的路由配置
- `Router02()` - 返回第二个额外服务器的路由配置

## 结构说明
此包作为handlers和middleware包的协调者，将路由与相应的处理函数连接起来。