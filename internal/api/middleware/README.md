# Middleware 包

该包包含Gin框架的中间件实现，用于横切关注点的处理。

## 功能
- 请求处理时间统计
- 响应体日志记录
- 可以扩展以添加认证、授权、错误处理等中间件

## 主要中间件
- `StatCost()` - 记录接口处理耗时，打印请求路径和处理函数名
- `GinBodyLogMiddleware()` - 记录HTTP响应体内容，便于调试和监控

## 使用方式
在路由设置中使用`router.Use()`方法添加这些中间件。